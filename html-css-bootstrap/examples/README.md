# Using these examples

HTML and CSS are easy to use; these examples require very little setup. Simple download the files and open `index.html` in your browser (usually you can just double click and it will open it in the browser by default). 
